CREATE OR REPLACE PROCEDURE USPU_R_MODEL_FILTER_DTL_VS2 (
    v_MODEL_ID NUMBER,
    v_VARIABLE_NAME VARCHAR2
)
AS
BEGIN
    UPDATE R_MODEL_FILTER_DTL_PEN A
    SET A.VARIABLE_SELECTION = 1
    WHERE A.MODEL_ID = v_MODEL_ID
    AND A.VARIABLENAME = v_VARIABLE_NAME;
    COMMIT;
END;
CREATE OR REPLACE PROCEDURE USPS_PARTIAL_RUN_DETAIL
(
    V_HEADERID NUMBER DEFAULT 0,
    Cur_out OUT SYS_REFCURSOR
)
AS
BEGIN

    OPEN Cur_out FOR
      SELECT A.PKID, A.HEADERID, A.CUSTOMER_NUMBER, A.ACCOUNT_NUMBER, A.REASON FROM IFRS_PARTIAL_RUN_DETAIL A
    WHERE HEADERID = V_HEADERID;

END;
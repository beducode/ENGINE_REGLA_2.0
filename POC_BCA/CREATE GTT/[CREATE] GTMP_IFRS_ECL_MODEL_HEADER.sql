CREATE TABLE IFRS9_BCA.GTMP_IFRS_ECL_MODEL_HEADER (
    PKID             NUMBER(22),
    SYSCODE_ECL      NVARCHAR2(44),
    ECL_MODEL_NAME   NVARCHAR2(50),
    EFFECTIVE_DATE   DATE,
    ACTIVE_FLAG      NUMBER(22),
    RUN_STATUS       NVARCHAR2(40),
    CREATEDBY        NVARCHAR2(72) DEFAULT 'SYSTEM',
    CREATEDDATE      TIMESTAMP(8) DEFAULT SYSTIMESTAMP,
    CREATEDHOST      NVARCHAR2(60) DEFAULT 'LOCALHOST',
    UPDATEDBY        NVARCHAR2(72),
    UPDATEDDATE      TIMESTAMP(8),
    UPDATEDHOST      NVARCHAR2(60)
);


CREATE SEQUENCE IFRS9_BCA.SEQ_GTMP_IFRS_ECL_MODEL_HEADER
START WITH 1
INCREMENT BY 1
NOCACHE
NOCYCLE;


CREATE OR REPLACE TRIGGER IFRS9_BCA.TRG_GTMP_IFRS_ECL_MODEL_HEADER_PKID
BEFORE INSERT ON IFRS9_BCA.GTMP_IFRS_ECL_MODEL_HEADER
FOR EACH ROW
WHEN (NEW.PKID IS NULL)
BEGIN
    SELECT IFRS9_BCA.SEQ_GTMP_IFRS_ECL_MODEL_HEADER.NEXTVAL
    INTO :NEW.PKID
    FROM DUAL;
END;